import React from "react";

type ManualImportProps = {
	isOpen: boolean;
	onOpenChange: (isOpen: boolean) => void;
};

const ManualImport = () => {
	return <div>ManualImport</div>;
};

export default ManualImport;
